Link do problema:

https://leetcode.com/problems/array-prototype-last/description/

Solução:

Array.prototype.last = function() {
    if(this.length <= 0){
        return -1;
    }

    const ultimaPosicao = this.length - 1;
    return this[ultimaPosicao];
};
